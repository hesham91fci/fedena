



<ul id="employee-list">
<% unless @employees.empty? %>
  <li class="heading"> Jméno zaměstnance </li>
    <%- @employees.each do |e| -%>
      <li class="list<%=cycle('even','odd')  %>">
        <div class="employee-name"><%= link_to "#{e.first_name}", :controller=>"employee", :action=>"create_monthly_payslip", :id=>e.id %></div>
      </li>
     <% end %>

  <% else %>
<p class="flash-msg">Žádný zaměstnanec nenalezen pod touto kategorií</p>
  <% end %>
</ul>
