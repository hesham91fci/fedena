

<ul id="category-list">
<% unless @categories.empty? %>
  <li class="listheading"> <div class="category-name">Aktivní Kategorie </div> </li>
    <%- @categories.each do |c| -%>
      <li class="list<%=cycle('odd', 'even')%>">
        <div class="category-name"><%= c.name %> (<%= c.prefix %>)</div>
        <div class="category-edit"><%= link_to "Upravit", {:action => "edit_category", :id=>c.id} %></div>
        <div class="category-edit"><%= link_to "Smazat", {:action => "delete_category", :id=>c.id},:confirm=>"Jste si jistí?" %></div>
      </li>
     <% end %>
     <% unless @inactive_categories.empty? %>
      <li class="listheading"><div class="category-name"> Neaktivní Kategorie</div></li>
    <%- @inactive_categories.each do |c| -%>
      <li class="list<%=cycle('odd', 'even')%>">
        <div class="category-name"><%= c.name %> (<%= c.prefix %>)</div>
        <div class="category-edit"><%= link_to "Upravit", {:action => "edit_category", :id=>c.id} %></div>
        <div class="category-edit"><%= link_to "Smazat", {:action => "delete_category", :id=>c.id},:confirm=>"Jste si jistí?" %></div>
      </li>
     <% end %>
     <% end %>
  <% else %>
<p class="flash-msg">Žádná kategorie zaměstnance nenalezena</p>
  <% end %>
</ul>

 
