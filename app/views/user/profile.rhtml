<div id="content-header">
  <img src="/images/user_profile/user_profile_logo.jpg" />
  <h1>Profil uživatele</h1>
  <h3>Informace o uživateli</h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

<% if @current_user.id == @user.id %>
  <div id="inner-tab-menu">
    <ul><li><%= link_to "Změnit heslo", :controller => "user", :action => "change_password" %></li></ul>
  </div>
<% end %>

<div id="page-yield">

  <% unless flash[:notice].nil? %><p class="flash-msg"> <%= flash[:notice] %> </p><% end %>

  <div id="profile_view">

    <div class="profile_info_label">Ev. číslo</div>
    <div class="profile_info_text"> <%= @user.username %> </div>

    <div class="profile_info_label2">Jméno a příjmení</div>
    <div class="profile_info_text2"> <%= @user.full_name %> </div>


    <div class="profile_info_label">Pozice</div>
    <div class="profile_info_text"> <%= @user.role_name %> </div>

    <div class="profile_info_label2">Email</div>
    <div class="profile_info_text2"> <%= @user.email %> </div>
    <div class="extender"></div>
  </div>

  <% if @current_user.admin? %>
    <div class="submit-buttons">

      <% if @user.role_name == 'Employee' %>
        <% if @config.include?('HR') %>
          <%= link_to "► Ukaž profil",{:controller => "employee", :action => "profile", :id => @employee.id }, :class => "user_button" unless @employee.nil?%>
        <% end %>
      <% elsif @user.role_name == 'Student' %>
        <%= link_to "► Ukaž profil",{:controller => "student", :action => "profile", :id => @student.id }, :class => "user_button"%>
      <% else %>
        <% if @config.include?('HR') %>
          <%= link_to "► Ukaž profil",{:controller => "employee", :action => "profile", :id => @employee.id }, :class => "user_button" unless @employee.nil?%>
        <% end %>
      <% end %>
      <%#= link_to "► Upravit",{:controller => "user", :action => "edit", :id => @user.username }, :class => "user_button"%>
      <%= link_to "Změnit heslo", {:controller => "user", :action => "user_change_password", :id =>@user.username}, :class => "user_button" %>
      <%# unless @current_user.id == @user.id %>
        <%#= link_to "► Smazat", { :controller => "user", :action => "delete", :id => @user.username}, :class => "user_button", :confirm=>"Are you sure you want to delete this USER?" %>
      <%# end %>
      <% unless @user.admin %>
        <% unless @user.role_name == 'Student' %>
          <%= link_to "► Upravit Práva", {:controller => "user", :action => "edit_privilege", :id => @user.username } ,:class => "user_button" %>
      
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>