<ul id="tutor-list">
  <% @emps_assigned =  @batch.employee_id.split(",") unless @batch.employee_id.nil?%>
  <% unless @employees.empty? %>
    <li class="heading"> Připojit nový: </li>
    <% @employees.each do |e| %>

      <li class="list<%=cycle('even', 'odd')%>">
        <div class="tutor-name"><%= e.full_name %></div>
        
          <% if @emps_assigned.nil? or not @emps_assigned.include?("#{e.id}")  %>
            <div class="remove"><%= link_to_remote "Přidat", :url => {:action => "assign_employee", :id => e.id,:batch_id=>params[:batch_id],:department_id=>params[:department_id]}, :update => "department-select" %></div>
          <% else %>
            <div class="remove"><%= link_to_remote "Odebrat", :url => {:action => "remove_employee", :id => e.id,:batch_id=>params[:batch_id],:department_id=>params[:department_id]}, :update => "department-select" %></div>
          <% end %>
        
      </li>

    <% end %>
  <% else %>
    <p class="flash-msg"> Zaměstnance nenalezen.</p>
  <% end %>

</ul>
